apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boardgame-chart.fullname" . }}-test-connection"
  labels:
    {{- include "boardgame-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boardgame-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
